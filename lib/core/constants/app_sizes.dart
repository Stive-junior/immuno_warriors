// Sizes and dimensions for Immuno Warriors.
//
// This file defines constant sizes for UI consistency across screens and widgets.
// Includes padding, margins, font sizes, and feature-specific dimensions.
class AppSizes {
  /// --- Padding ---
  /// Small padding for tight spaces (8.0).
  static const double paddingSmall = 8.0;

  /// Medium padding for standard spacing (16.0).
  static const double paddingMedium = 16.0;

  /// Large padding for prominent elements (24.0).
  static const double paddingLarge = 24.0;

  /// Extra large padding for dialogs and cards (32.0).
  static const double paddingExtraLarge = 32.0;

  /// Dialog padding for content (20.0).
  static const double dialogPadding = 20.0;

  /// Dialog button horizontal padding (16.0).
  static const double dialogButtonPaddingHorizontal = 16.0;

  /// Dialog button vertical padding (12.0).
  static const double dialogButtonPaddingVertical = 12.0;

  /// Button horizontal padding for actions (20.0).
  static const double buttonPaddingHorizontal = 20.0;

  /// Button vertical padding for actions (10.0).
  static const double buttonPaddingVertical = 10.0;

  /// --- Margin ---
  /// Small margin for subtle spacing (8.0).
  static const double marginSmall = 8.0;

  /// Medium margin for standard separation (16.0).
  static const double marginMedium = 16.0;

  /// Large margin for prominent separation (24.0).
  static const double marginLarge = 24.0;

  /// --- Card ---
  /// Default border radius for cards (20.0).
  static const double defaultCardRadius = 20.0;

  /// Default border width for cards (2.0).
  static const double defaultBorderWidth = 2.0;

  /// Default elevation for card shadows (10.0).
  static const double defaultElevation = 10.0;

  /// Height for user profile cards (200.0).
  static const double userCardHeight = 200.0;

  /// Minimum width for cards (180.0).
  static const double cardMinWidth = 180.0;

  /// Combat card height for combat units (150.0).
  static const double combatCardHeight = 150.0;

  /// Research node card width for research tree (120.0).
  static const double researchNodeWidth = 120.0;

  /// --- Avatar ---
  /// Default avatar size for profiles (60.0).
  static const double avatarSize = 60.0;

  /// Size for add user Lottie animation (100.0).
  static const double addUserLottieSize = 100.0;

  /// Combat unit avatar size for combat screen (80.0).
  static const double combatAvatarSize = 80.0;

  /// --- Button ---
  /// Default button height for actions (50.0).
  static const double buttonHeight = 50.0;

  /// Default button border radius (12.0).
  static const double buttonRadius = 12.0;

  /// --- Icon ---
  /// Small icon size for subtle UI elements (18.0).
  static const double iconSizeSmall = 18.0;

  /// Medium icon size for standard icons (24.0).
  static const double iconSizeMedium = 24.0;

  /// Large icon size for prominent icons (32.0).
  static const double iconSizeLarge = 32.0;

  /// Icon button size for interactive icons (60.0).
  static const double iconButtonSize = 60.0;

  /// Lottie icon size for animations (40.0).
  static const double iconLottieSize = 40.0;

  /// Error icon size for error dialogs (80.0).
  static const double errorIconSize = 80.0;

  /// Combat action icon size for combat buttons (36.0).
  static const double combatActionIconSize = 36.0;

  /// --- Font ---
  /// Small font size for captions (12.0).
  static const double fontSizeSmall = 12.0;

  /// Medium font size for body text (16.0).
  static const double fontSizeMedium = 16.0;

  /// Large font size for subtitles (20.0).
  static const double fontSizeLarge = 20.0;

  /// Extra large font size for headings (24.0).
  static const double fontSizeExtraLarge = 24.0;

  /// Heading font size for main titles (32.0).
  static const double fontSizeHeading = 32.0;

  /// Dialog title font size for dialogs (26.0).
  static const double dialogTitleSize = 26.0;

  /// Dialog option text font size (18.0).
  static const double dialogOptionTextSize = 18.0;

  /// Error text font size for error messages (22.0).
  static const double errorTextSize = 22.0;

  /// Error detail text font size for error details (16.0).
  static const double errorDetailTextSize = 16.0;

  /// Error button text font size for error dialogs (18.0).
  static const double errorButtonTextSize = 18.0;

  /// Combat log text size for combat logs (14.0).
  static const double combatLogTextSize = 14.0;

  /// --- Breakpoints ---
  /// Mobile breakpoint for responsive design (600.0).
  static const double mobileBreakpoint = 600.0;

  /// Tablet breakpoint for responsive design (1000.0).
  static const double tabletBreakpoint = 1000.0;

  /// --- Miscellaneous ---
  /// Circular indicator size for loading (40.0).
  static const double circularIndicatorSize = 40.0;

  /// Application logo size for splash and auth (120.0).
  static const double logoSize = 120.0;

  /// Form field height for input fields (55.0).
  static const double formFieldHeight = 55.0;

  /// Form field border radius for inputs (10.0).
  static const double formFieldRadius = 10.0;

  /// Extra small spacing for tight layouts (4.0).
  static const double spaceXSmall = 4.0;

  /// Small spacing for standard gaps (8.0).
  static const double spaceSmall = 8.0;

  /// Medium spacing for balanced layouts (16.0).
  static const double spaceMedium = 16.0;

  /// Large spacing for prominent separation (24.0).
  static const double spaceLarge = 24.0;

  /// Extra large spacing for major sections (32.0).
  static const double spaceExtraLarge = 32.0;

  /// Logo width for branding (150.0).
  static const double logoWidth = 150.0;

  /// Logo height for branding (80.0).
  static const double logoHeight = 80.0;

  /// Health bar height for combat UI (10.0).
  static const double healthBarHeight = 10.0;

  /// Threat scanner animation size (200.0).
  static const double threatScannerAnimationSize = 200.0;
}
